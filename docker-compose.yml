services:
  db:
    image: postgres:15-alpine
    container_name: jobai-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: jobai
      POSTGRES_PASSWORD: password
      POSTGRES_DB: JOBAI-DB
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
